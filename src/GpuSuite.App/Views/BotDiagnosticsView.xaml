<UserControl x:Class="GpuSuite.App.Views.BotDiagnosticsView"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:GpuSuite.App.ViewModels"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance vm:BotDiagnosticsViewModel}"
             Background="Transparent">
    <UserControl.Resources>
        <Style x:Key="DiagnosticStatusTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="Failed">
                    <Setter Property="Foreground" Value="{StaticResource StatusInvalidBrush}"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="DiagnosticRowAccentStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="3,0,0,0"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="Failed">
                    <Setter Property="BorderBrush" Value="{StaticResource StatusInvalidBrush}"/>
                    <Setter Property="Background" Value="#1F171B"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="DiagnosticStatusCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource SurfaceAltBrush}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="10"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="Failed">
                    <Setter Property="BorderBrush" Value="{StaticResource StatusInvalidBrush}"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="Background" Value="#24191D"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid Margin="28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Margin="0,0,0,14">
            <StackPanel>
                <TextBlock Text="Bot diagnostics" Style="{StaticResource H1TextStyle}"/>
                <TextBlock Text="{Binding Summary}" Style="{StaticResource CaptionTextStyle}" Margin="0,4,0,0"/>
            </StackPanel>
            <Button Content="Refresh evidence" Command="{Binding LoadCommand}" HorizontalAlignment="Right" VerticalAlignment="Top"/>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="380"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}" Margin="0,0,16,0">
                <ListBox ItemsSource="{Binding Rows}" SelectedItem="{Binding SelectedRow, Mode=TwoWay}"
                         Style="{StaticResource RowsListBoxStyle}" ItemContainerStyle="{StaticResource GameRowItemStyle}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource DiagnosticRowAccentStyle}" Padding="9,0,0,0">
                            <StackPanel>
                                <Grid>
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding Status}" HorizontalAlignment="Right" Style="{StaticResource DiagnosticStatusTextStyle}" FontSize="12"/>
                                </Grid>
                                <TextBlock Text="{Binding RunSummary}" Margin="0,5,0,0" FontSize="11"
                                           Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap"/>
                            </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel DataContext="{Binding SelectedRow}">
                        <TextBlock Text="{Binding Name}" Style="{StaticResource H2TextStyle}"/>
                        <TextBlock Text="{Binding GameId}" Style="{StaticResource CaptionTextStyle}" Margin="0,3,0,14"/>
                        <Grid Margin="0,0,0,14">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Style="{StaticResource DiagnosticStatusCardStyle}" Margin="0,0,5,0">
                                <StackPanel><TextBlock Text="STATUS" Style="{StaticResource LabelTextStyle}" Margin="0"/><TextBlock Text="{Binding Status}" Style="{StaticResource DiagnosticStatusTextStyle}" Margin="0,4,0,0"/></StackPanel>
                            </Border>
                            <Border Grid.Column="1" Background="{StaticResource SurfaceAltBrush}" CornerRadius="8" Padding="10" Margin="5,0">
                                <StackPanel><TextBlock Text="FAILURE CLASS" Style="{StaticResource LabelTextStyle}" Margin="0"/><TextBlock Text="{Binding FailureClass}" Margin="0,4,0,0"/></StackPanel>
                            </Border>
                            <Border Grid.Column="2" Background="{StaticResource SurfaceAltBrush}" CornerRadius="8" Padding="10" Margin="5,0,0,0">
                                <StackPanel><TextBlock Text="RUNS" Style="{StaticResource LabelTextStyle}" Margin="0"/><TextBlock Text="{Binding RunSummary}" Margin="0,4,0,0" TextWrapping="Wrap"/></StackPanel>
                            </Border>
                        </Grid>
                        <TextBlock Text="LATEST FINDING" Style="{StaticResource LabelTextStyle}"/>
                        <TextBlock Text="{Binding Detail}" TextWrapping="Wrap" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,14"/>
                        <TextBlock Text="LAST CHECKED (UTC)" Style="{StaticResource LabelTextStyle}"/>
                        <TextBlock Text="{Binding CheckedUtc, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,14"/>
                        <TextBlock Text="LAST SUCCESSFUL ROUTE" Style="{StaticResource LabelTextStyle}"/>
                        <TextBlock Text="{Binding LastSuccessfulRoute}" TextWrapping="Wrap" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,14"/>
                        <TextBlock Text="MEASURED MOTION" Style="{StaticResource LabelTextStyle}"/>
                        <TextBlock Text="{Binding MotionSummary}" TextWrapping="Wrap" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,5,0,14"/>
                        <TextBlock Text="LATEST EVIDENCE" Style="{StaticResource LabelTextStyle}"/>
                        <TextBlock Text="{Binding LatestEvidence}" TextWrapping="Wrap" Foreground="{StaticResource TextMutedBrush}" FontSize="11" Margin="0,5,0,14"/>
                        <StackPanel Orientation="Horizontal" DataContext="{Binding DataContext, ElementName=Root}">
                            <Button Content="Open evidence folder" Command="{Binding OpenEvidenceCommand}" Margin="0,0,8,0"/>
                            <Button Content="Open latest evidence" Command="{Binding OpenLatestEvidenceCommand}"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl>
